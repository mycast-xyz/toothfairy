{
	"name": "toothfairy-socket-server",
	"version": "1.0.0",
	"description": "ToothFairy CAM Print 실시간 소켓 서버",
	"main": "socket-server-example.js",
	"scripts": {
		"start": "node socket-server-example.js",
		"dev": "nodemon socket-server-example.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": ["websocket", "socket", "realtime", "cam", "print", "toothfairy"],
	"author": "ToothFairy Team",
	"license": "MIT",
	"dependencies": {
		"ws": "^8.14.2"
	},
	"devDependencies": {
		"nodemon": "^3.0.2"
	},
	"engines": {
		"node": ">=16.0.0"
	}
}
